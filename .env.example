# 複製成 .env 並填入真的值，.env 不會被 commit。
# 不填的話預設用本機 SQLite 檔案(matsuboard.db)，不影響開發。
#
# Supabase 連線字串請用「Connection pooling -> Transaction」分頁那個
# (host 是 aws-0-<region>.pooler.supabase.com:6543)，不要用 Direct connection
# 那個 (db.<ref>.supabase.co:5432) —— 那個 host 現在只有 IPv6，本機/多數環境連不上。
DATABASE_URL=postgresql://postgres.<project-ref>:<password>@aws-0-<region>.pooler.supabase.com:6543/postgres
